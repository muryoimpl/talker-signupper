import React from 'react';

class TalkButton extends React.Component {
  constructor(props) {
    super(props)
    this.handleClickOpen = props.handleClickOpen.bind(this);
  }

  render() {
    return (

    )
  }
}

export default TalkButton;
